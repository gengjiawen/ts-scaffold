{
  "name": "{{ name }}",
  "version": "1.0.0",
  "description": "{{ description }}",
  "author": "{{ author }}",
  "scripts": {
    "start": "nodemon --ext ts --exec ts-node index.ts",
    "clean": "rimraf build",
    "prettify": "prettier \"{examples,lib,script,test}/**/*.ts\" \"*.ts\" --write",
    "build": "npm run clean && tsc -p ./tsconfig.json"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@types/node": "^9.6.2",
    "rimraf": "^2.6.2",
    "nodemon": "^1.17.3",
    "prettier": "^1.11.1",
    "ts-node": "^5.0.1",
    "typescript": "^2.8.1"
  }
}
