{
  "name": "{{ name }}",
  "version": "1.0.0",
  "description": "{{ description }}",
  "author": "{{ author }}",
  "scripts": {
    "start": "nodemon --ext ts --exec ts-node index.ts"
  },
  "dependencies": {
  },
  "devDependencies": {
    "nodemon": "^1.12.0",
    "ts-node": "^3.3.0",
    "typescript": "^2.4.2"
  }
}
